# Extracting-and-Converting-Numeric-Data-from-a-String
A Python program that extracts a numeric value embedded within a text string and converts it into a floating-point number. The project demonstrates string searching, slicing, type conversion, and basic data parsing techniques commonly used when processing formatted text data.
